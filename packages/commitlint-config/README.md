# Commitlint of taoism

## Install

```shell
# with npm
npm i @taoism/commitlint-config -D

# with yarn
yarn add @taoism/commitlint-config -D

# with pnpm
pnpm add @taoism/commitlint-config -D
```

## Usage

commitlint.config.js

```shell
{
	extends: ['@taoism/commitlint-config']
}
```
